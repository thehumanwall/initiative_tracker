<h1>Teams</h1>

<%= link_to "New team", new_team_path %>

<div id="teams">
  <% @teams.each do |team| %>
  <div>
    <a href="/teams/<%= team.id %>">
      <%= team.name %>
    </a>
  </div>
  <% end %>
</div>